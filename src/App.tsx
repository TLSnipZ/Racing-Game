import{useState}from'react';import{Gauge,Map,Warehouse,Wrench,CarFront,ChevronRight}from'lucide-react';import{STARTER_CARS,StarterCar}from'./data/starters';
const yen=(n:number)=>`¥${n.toLocaleString('en-US')}`;
export function App(){const[selected,setSelected]=useState<StarterCar|null>(null);return <main className="shell"><header><div><div className="eyebrow">KAGEHAMA / 01:47 JST</div><h1>KAGEHAMA</h1><p className="subtitle">UNDERGROUND CAR EMPIRE</p></div><div className="cash">STARTING CASH <strong>¥50,000</strong></div></header><section className="hero"><div className="heroCopy"><span className="tag">MERCER GARAGE // EAST WARD</span><h2>Everybody starts<br/>with a bad decision.</h2><p>Three unwanted cars. Fifty thousand yen. One way into Kagehama's midnight scene.</p><div className="message"><b>UNKNOWN</b><span>Heard you're looking for a ride. Got three cars nobody wants. Midnight. Don't be late.</span></div></div><div className="city"><div className="sun"/><div className="road">首都高速 • EASTLINE</div></div></section><section className="choice"><div className="sectionTitle"><div><span>01 / FIRST RIDE</span><h3>Choose your starter</h3></div><p>The other two can appear on the used market later.</p></div><div className="cards">{STARTER_CARS.map((car,i)=><button key={car.id} className={'carCard '+(selected?.id===car.id?'selected':'')} onClick={()=>setSelected(car)}><div className="cardTop"><span>0{i+1}</span><span>{car.archetype}</span></div><div className={'silhouette s'+i}><CarFront size={78}/></div><h4>{car.name}</h4><div className="year">{car.year} · {car.engine} · {car.drive}</div><p>{car.description}</p><div className="stats"><span><b>{car.hp}</b> PS</span><span><b>{car.weightKg}</b> KG</span><span><b>{car.condition}%</b> COND.</span></div><div className="traits">{car.traits.map(t=><span key={t}>{t}</span>)}</div><div className="price"><span>{yen(car.priceYen)}</span><small>{yen(50000-car.priceYen)} REMAINS</small></div></button>)}</div>{selected&&<div className="confirm"><div><span>SELECTED</span><strong>{selected.name}</strong><small>Your first save will be created after confirmation.</small></div><button onClick={()=>alert(`Phase 0 preview — ${selected.name} selected. Persistent saves arrive in Phase 2.`)}>ENTER KAGEHAMA <ChevronRight size={18}/></button></div>}</section><nav><span><Warehouse/>Garage</span><span><Map/>City</span><span><Gauge/>Races</span><span><Wrench/>Workshop</span></nav><footer>PHASE 0 // FOUNDATION BUILD · PRE-ALPHA</footer></main>}
